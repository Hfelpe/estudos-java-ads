public class Carro {
    // Atributos (O que o carro TEM)
    String modelo;
    String placa;

    // Método (O que o carro FAZ)
    public void ligar() {
        System.out.println("O carro " + modelo + " está ligado!");
    }
}
